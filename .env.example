# ==========================================
# OMNIMED-AGENT-OS: ENVIRONMENT VARIABLES
# Copy this file to '.env' and fill in your values
# ==========================================

# OpenAI API Key 
# (Leave as default to bypass LangChain warnings if running 100% locally with Unsloth/Llama)
# (Replace with your actual key ONLY IF you plan to use OpenAI as a cloud fallback)
OPENAI_API_KEY=fake_key_to_bypass_langchain_warnings

# Add future configurations below:
# HUGGINGFACE_TOKEN=your_hf_token_here
# GRADIO_SERVER_PORT=7860
